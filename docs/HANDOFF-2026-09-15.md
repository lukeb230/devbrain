# DevBrain handoff — 2026-09-15

Written to end one session and start the next cleanly. Read this, then `git -C ~/Downloads/devbrain-product log --oneline -8` and the memory index.

## One-line state

`main` is clean and pushed (`fdef925`), CI green, production (getdevbrain.com) is serving `fdef925`, both test VMs are on app 0.4.12. **A later session built the support channel on branch `feat/support-reports` — see the section below; it waits on Luke's Resend steps.** The app-testing fix work is fully merged, deployed, and partly re-verified live. Since the first draft of this handoff: the panel Console button shipped, prompt caching on the system prompt shipped, and the beta cost model was worked out. Luke reported not seeing the new panel button — it is deployed; his panel just needs a reload (see below).

## What shipped after the handoff was first written (2026-09-15)

- **Prompt caching on the system prompt** (`fdef925`, live). `cachedSystem()` in `src/lib/agent.ts` marks the system prompt as an ephemeral-cache block on both `askClaude` and `askClaudeBlocks`; `record()` folds the cache counters into the usage total. Pure cost/latency win, no quality change, billing is per-call not per-token so it's untouched. Two tests added; 369 green.
- **Brain-docs caching: considered and declined** (Luke's call). Reason: PR reviews don't send brain docs at all, and where brain docs are sent (the spec-assessment flow, `src/app/api/agents/tick/route.ts:351`) they're concatenated with volatile data (task board, merged PRs, file tree), so cross-call cache hits would be rare and a naive cache could cost more via the write premium. The one safe win — caching the corpus prefix across the spec-assessment batch loop — was offered and declined. Do not implement without Luke asking.
- **Beta cost model decided.** AI cost is per team (metered by `ai_usage.calls`), ~$0.05/action blended on Sonnet 4.5 (no caching baseline), Base cap 40 actions/day ≈ ~$60/team/month worst case. To hold the free-beta AI bill under $2,000/month: recommended `beta.max_teams` ≈ 50, per-team `ai_daily_cap` = 40, and the global backstop `system_state.ai_limits.global_daily` ≈ 1,300 (currently 5,000). These are recommendations, NOT yet applied — flip with `scripts/beta.mjs` + SQL when Luke decides. Tune from the real token sums in `ai_usage` after a week.
- **Roadmap artifact refreshed** — https://claude.ai/code/artifact/5845961d-8c6d-4c6e-acea-e2a85804ed96 (the "DevBrain Launch Roadmap") updated to the 2026-09-15 state.

## Support channel — built, not yet live (2026-09-15, later session)

Branch `feat/support-reports` (13 commits over `9abf83a`, head `eab3a75`), **not merged, not pushed** — Luke decides. Spec `docs/superpowers/specs/2026-09-15-support-reports-design.md`, plan `docs/superpowers/plans/2026-09-15-support-reports.md` (Tasks 1–7 done, subagent-driven, per-task review + whole-branch review; 404 tests, typecheck and build green). What it is: `/support` on the website (question / bug / feature), `/desk/help` in the Console (bug / feature, app context attached), every submission a `reports` row, two Resend emails per report (ops copy reply-to the submitter; ack to the submitter reply-to team@), mail failure → row `failed` + ops alert `support_mail`. Privacy page discloses it. `RESEND_API_KEY` is a recommended env var.

**Already done to production:** migration `0043_reports` applied to Supabase (table exists, 0 rows, RLS on). Additive; harmless if the branch never ships.

**Luke's three steps before Task 8 (the live gate):**
1. Accept Resend's marketplace terms: https://vercel.com/dev-brain1/~/integrations/accept-terms/resend?source=cli — then re-run `cd ~/Downloads/devbrain-product && vercel integration add resend/resend-email` (the CLI returned `action_required` and stopped; nothing is installed yet, `vercel integration list` shows no resources).
2. In the Resend dashboard add the domain `getdevbrain.com`, put its DNS records (DKIM + return-path CNAME) at the DNS host, wait for Verified.
3. `vercel env ls` must show `RESEND_API_KEY` for Production and Preview; copy it into `.env.local` for local runs. **Do not merge/deploy before this** — `/support` goes public on deploy and its success copy says "we've emailed a copy to you", which is untrue until the key exists (reports would still be stored, with an ops alert every 6 h).

**Then Task 8** per the plan: one submission of each kind from the site, one bug from a VM Console, one deliberate failure with the key blanked to see the native "Support mail is not going out" alert, restore, confirm recovery. Extra check from the final review: the ops copy is From team@ To team@ via Resend — if team@ is Google Workspace, confirm it lands in the inbox, not spam (DMARC alignment via Resend's DKIM).

**Known, accepted:** the Resend v6 SDK exposes no abort signal, so the 5 s budget is a timer race and a slow-but-successful send is recorded as `failed` (false alarm, never a lost report). Deferred minors are listed in the plan's final-review triage (aria-describedby on non-message fields, `core()` bridge accessor duplicated from mac-settings, a few untested action branches, fixed two-column grid on `/desk/help`).

## Ground rules (do not skip)

- Product repo: `~/Downloads/devbrain-product` (GitHub `lukeb230/devbrain`). **The shell cwd resets to `~/Downloads/devbrain` after every command — that is the FROZEN FlowSync repo. Never read or write it. Always `cd ~/Downloads/devbrain-product` first.**
- Production: `https://getdevbrain.com`. Supabase project `guuzgqzljrnfqzrprgrp`. Vercel team `dev-brain1`, project `devbrain` (`prj_edhlqxpLfJ6WCWYBsZeopROULWdK`, org `team_DwneO63TQx8ln7uyPdHT1bOg`).
- `git push` works from this environment now (used it this session), but Luke usually pushes. Confirm before pushing.
- Never handle passwords or sign in to accounts for Luke. Do not raise notarization (deliberately last).
- Commit trailer for this project (per the latest session reminder):
  ```
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  Claude-Session: <the session URL the harness gives you>
  ```
- CI has a **`plugin-beta is in sync with plugin`** check: after ANY edit under `plugin/`, run `./tools/sync-beta-plugin.sh && git add plugin-beta` and commit it, or CI Tests goes red. This bit us this session.

## What shipped this session

1. **App-testing fix branch** — `docs/superpowers/plans/2026-09-14-app-testing-fixes.md`, Tasks 1–17, executed subagent-driven in a worktree, reviewed per-task + a final whole-branch review, merged to `main` (fast-forward) and pushed. Commits `bf0eb6c … 0769cd7` (23 of them). Covers: dead Console tokens + double-submit (T1), team-scoped token list (T2), `devbrain bootstrap` copy (T3), per-machine "Set up this Mac" (T4–6), Codex 3s hook + doctor line (T7), legacy-host migration on update (T8), team creation in the Console instead of Safari (T9–10), docs/site copy (T11), Codex `apply_patch` seen by the guard (T14), one live session per teammate (T15), team-switcher click + says-so-on-failure (T16), Console window fits the display's work area (T17, app → 0.4.12).
2. **Final-review fixes** (`0769cd7`): scoped the session supersede to `user_id`, made the no-team panel refresh itself, fixed the `token_label_taken` copy, the `my-mac` hostname hoist, and the HANDOFF test count.
3. **plugin-beta sync** (`9eb1a64`) — fixed the red CI check the merge push caused.
4. **Panel Console button** (`cfbe81e`) — replaced the wide "Open the Console board · PRs · brain · feed · team →" bar with a small open-in-window icon button in the tab row's inner corner. It reads the widget's screen corner from the shell's `get_corner` command and follows the `corner-changed` event live: bottom-right widget → button at the panel's top-left; bottom-left → top-right. Pure web change, shipped on deploy, no app build. File: `src/app/widget/app.tsx`.

## Live verification done (Task 13, partial)

Verified over ssh against the two VMs and `sandbox240/neap` (recorded in `docs/APP-TESTING-2026-09-14.md` under "Fix verification"):
- **PASS** T7 (Codex 3s hook, doctor line, no clamp warning), T8 (server host), T14 (collision hook fed a Codex `apply_patch` body returns "ask" naming the file — was silent before), T15 (three Cursor chats → one live session), T17 (Console window 1024×681 at (0,30) on a 1024×768 screen, ~57px Dock clearance).

## Open / next steps

1. **Panel button not visible to Luke (top of list).** It IS deployed (`cfbe81e` is on getdevbrain.com — confirmed via GitHub's Production deployment record). His panel is showing a cached copy. Fix on his side: DevBrain menu-bar icon → "Reload panel", or quit and reopen the app. If he reloads and it is STILL missing, investigate for real: check the deployed panel chunk actually contains the button (it is behind auth, so grep the authenticated bundle or drive a VM panel), and confirm `get_corner`/`corner-changed` work in his installed app. Do not assume it is just cache a second time.
2. **Task 13 remainder — needs Luke at the screen (or careful VNC driving).** Four checks not yet run because they need the Console UI clicked or a fresh GitHub account: tokens page (mint → Copy → curl 200; re-mint shows "already exists"; switch-teams changes the list), "Set up this Mac" across teams, the team switcher (five clicks, ✓ visible), and fresh-account sign-in landing in the Console (TeamWall + wall, Safari untouched). The logic under each is unit-tested and green; this is confirmation. Offer to drive the three Console ones over VNC while Luke watches; the sign-in one needs Luke to enter the new account.
3. **Parked follow-ups from the final review** (each wants its own small task + review seat):
   - The panel's own `SetupScreen` (`src/app/widget/app.tsx`) still reuses the on-disk token when you pick a different team — this is the same BUG 2 that T5/T6 fixed for the Console wall, still live on the panel. Fix mirrors `mac-step.tsx`: always mint for the selected team.
   - Codex multi-file `apply_patch` — `editedFile` (`plugin/hooks/host.mjs`) returns only the first `*** Update File:` path, so a multi-file patch is guarded/recorded for file 1 of N only. Follow-up: an `editedFiles()` that returns all, the collision hook loops and denies on the first hit.
   - The panel's team Picker (`src/app/widget/app.tsx`) still calls `switchOrg` programmatically; give it the same `pickOrg` + full-navigation treatment T16 gave the Console.
   - Deferred minors are listed in the plan and the (now-deleted) SDD ledger; none block.
4. **Launch finish-line** (from the checkpoint memory, roughly in order):
   - **Alert delivery is still dark** — the ops webhook and `system_state.ops_webhook` row. Top open item; nothing pages Luke when production breaks. This is Luke's ~30 min but he has been putting it off.
   - Anthropic key on the work account, test-data cleanup, confirm `team@getdevbrain.com` receives mail.
   - Notarization stays LAST, only when Luke raises it.

## Tools / how to drive things

- **VM ssh helper** (created this session): `/private/tmp/.../scratchpad/vm.sh <ip> '<cmd>'` — a one-shot `sshpass -p admin ssh …` with the right flags. If the scratchpad is gone, the raw command is: `sshpass -p admin ssh -o StrictHostKeyChecking=no -o IdentitiesOnly=yes -o PreferredAuthentications=password -o NumberOfPasswordPrompts=1 admin@<ip>`. VMs: `cursor-mac` 192.168.64.8, `codex-mac` 192.168.64.9 (windowed `tart run`, no VNC by default). Codex CLI on codex-mac is at `/usr/local/bin/codex`, not on the default PATH over ssh — prefix `export PATH=/usr/local/bin:$HOME/.devbrain/bin:$PATH`. `codex exec --sandbox workspace-write --dangerously-bypass-hook-trust "<prompt>"`; `~/work/nw` = `sandbox240/neap` (linked to team northwind), `~/work/pg` = `lukeb230/devbrain-playground` (NOT linked — edits there post nothing).
- **Update a VM to latest:** `~/.devbrain/bin/node ~/.devbrain/src/cli/bin/devbrain.mjs update` (pulls source from main + installs the released widget).
- **CI / deploy checks:** `gh run list --branch main --limit 3`; production commit = `gh api "repos/lukeb230/devbrain/deployments?environment=Production&per_page=1" --jq '.[0].sha[0:7]'` (more reliable than `vercel ls`, whose "age" column misled me this session). `vercel` CLI is authed for scope `dev-brain1`; the Vercel MCP tools are NOT (403).
- **Panel is behind auth** — an unauthenticated fetch of `/widget` does not load the panel chunk, so you cannot grep production for panel code without a session.

## Repo facts worth keeping

- Widget only anchors **two** corners: `bl` / `br` (`widget/src-tauri/src/main.rs`, `enum Corner`). No top corners.
- `get_corner` and the `corner-changed` emit already existed in the shell — the panel button needed no Rust change.
- Tests: `npx vitest run` (367 green on `cfbe81e`), `npm run typecheck`, `npm run build`. Rust: `cd widget/src-tauri && cargo test`. A fresh worktree needs the git-ignored `widget/src-tauri/node/bin/node` present for `cargo` (the build script calls it) — clone it from the main checkout.

## Memory

`devbrain-checkpoint.md` was updated 2026-09-14 with the fix-branch state; update it again if you close out Task 13 or the launch items. The full plan and per-task detail live in `docs/superpowers/plans/2026-09-14-app-testing-fixes.md` and `docs/APP-TESTING-2026-09-14.md`.
