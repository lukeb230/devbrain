{
  "name": "devbrain",
  "description": "Connects this Claude to the team's DevBrain: live team context, collision checks, presence, and the shared second brain.",
  "version": "0.7.2",
  "author": {
    "name": "DevBrain"
  },
  "mcpServers": {
    "devbrain": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/mcp/server.mjs"
      ]
    }
  }
}
